---
# =============================================================================
# n8n Worker Deployment
# Processes jobs from the Redis Bull queue (EXECUTIONS_MODE=queue)
# Workers are stateless — no PVC needed
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n-worker
  namespace: n8n
  labels:
    app.kubernetes.io/name: n8n
    app.kubernetes.io/component: worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: n8n-worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: n8n-worker
        app.kubernetes.io/name: n8n
        app.kubernetes.io/component: worker
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "n8n"
        vault.hashicorp.com/agent-pre-populate-only: "false"
        vault.hashicorp.com/agent-revoke-on-shutdown: "true"

        vault.hashicorp.com/agent-inject-secret-core: "secret/data/n8n/core"
        vault.hashicorp.com/agent-inject-file-core: "core-env"
        vault.hashicorp.com/agent-inject-template-core: |
          {{- with secret "secret/data/n8n/core" -}}
          export N8N_ENCRYPTION_KEY="{{ .Data.data.encryption_key }}"
          {{- end }}

        vault.hashicorp.com/agent-inject-secret-pg: "secret/data/n8n/postgres"
        vault.hashicorp.com/agent-inject-file-pg: "pg-env"
        vault.hashicorp.com/agent-inject-template-pg: |
          {{- with secret "secret/data/n8n/postgres" -}}
          export DB_POSTGRESDB_PASSWORD="{{ .Data.data.password }}"
          {{- end }}

        vault.hashicorp.com/agent-inject-secret-redis: "secret/data/n8n/redis"
        vault.hashicorp.com/agent-inject-file-redis: "redis-env"
        vault.hashicorp.com/agent-inject-template-redis: |
          {{- with secret "secret/data/n8n/redis" -}}
          export QUEUE_BULL_REDIS_PASSWORD="{{ .Data.data.password }}"
          {{- end }}

        vault.hashicorp.com/agent-inject-secret-smtp: "secret/data/n8n/smtp"
        vault.hashicorp.com/agent-inject-file-smtp: "smtp-env"
        vault.hashicorp.com/agent-inject-template-smtp: |
          {{- with secret "secret/data/n8n/smtp" -}}
          export N8N_SMTP_PASS="{{ .Data.data.password }}"
          {{- end }}

        vault.hashicorp.com/agent-inject-secret-llm: "secret/data/n8n/llm"
        vault.hashicorp.com/agent-inject-file-llm: "llm-env"
        vault.hashicorp.com/agent-inject-template-llm: |
          {{- with secret "secret/data/n8n/llm" -}}
          export LLM_API_KEY="{{ .Data.data.api_key }}"
          {{- end }}

    spec:
      serviceAccountName: n8n-sa
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      initContainers:
        - name: wait-postgres
          image: busybox:1.36
          command: ["/bin/sh", "-c"]
          args:
            - |
              PG_IP=$(cat /config/pg-ip)
              until nc -z "$PG_IP" 5432 2>/dev/null; do
                echo "Waiting for PostgreSQL at $PG_IP..."; sleep 3
              done
              echo "PostgreSQL ready."
          volumeMounts:
            - name: svc-ips
              mountPath: /config
        - name: wait-redis
          image: busybox:1.36
          command: ["/bin/sh", "-c"]
          args:
            - |
              REDIS_IP=$(cat /config/redis-ip)
              until nc -z "$REDIS_IP" 6379 2>/dev/null; do
                echo "Waiting for Redis at $REDIS_IP..."; sleep 3
              done
              echo "Redis ready."
          volumeMounts:
            - name: svc-ips
              mountPath: /config
      containers:
        - name: n8n-worker
          image: n8nio/n8n:latest
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              for f in /vault/secrets/*-env; do
                [ -f "$f" ] && . "$f"
              done
              exec n8n worker
          envFrom:
            - configMapRef:
                name: n8n-config
          resources:
            requests:
              cpu: "200m"
              memory: "384Mi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
          volumeMounts:
            - name: vault-secrets
              mountPath: /vault/secrets
            - name: worker-tmp
              mountPath: /home/node/.n8n
      volumes:
        - name: vault-secrets
          emptyDir:
            medium: Memory
        - name: worker-tmp
          emptyDir: {}
        - name: svc-ips
          configMap:
            name: n8n-svc-ips

---
# =============================================================================
# HPA — auto-scale workers between 2 and 5 replicas
# Requires metrics-server in the cluster
# =============================================================================
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: n8n-worker-hpa
  namespace: n8n
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: n8n-worker
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
